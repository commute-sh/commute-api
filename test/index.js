const request = require('request');
const sinon = require('sinon');
const chai = require('chai');
const fs = require('fs');
const expect = require('chai').expect;

describe('Stations', () => {

    it('Should Find stations nearby', (done) => {
        done();
    });

});